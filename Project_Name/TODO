Events.js: Events are basically polling functions detecting the truthfullness of condtion(x,y) | x = get(APIx) y = POST(APIy,x) 
    [X] Finish API endpoint options 
        [X] Can use the response from the initial API call as the body or the params for the second api call. 
    [X] Custom Event functions 
        [X] the user can implement their own get,post,poll and helpper functions. 
    [ ] Write JSON data to the specified file
Event_Handlers.js: Event Handlers look through the specified file for events of a specified type, then execute some function with the response and date
    []  Implement functionality for reading the specified file, 


    